//CD HERE
// Let's make CD;
#include <windows.h>
#include <stdliboin>
#include "stdlb"
#include <InFTr>
#include <conc>
#include "cccore3"
#include "chanpath"
#include <stustupers>
// class without main or function first. . .
cclass::mejorCD{
 
  CreatePathFile("desktop");
  CreateMejor mejorfile = Pathfile("CCCore3");
  if(path == mejorfile)
  {
    makenewbat(0);
    loop(100);
    
  }
  else
  {
   loop(1); 
  }
 //showhidefunction() will b here
  mejor~>path(0.0);
  mejor~>file(1.0);
  mejor~>cinc(1.1);
  mejor.rpath = 0x0;
  //
  if(spath.mejors = TRUE || spath.mejors == spath.size(0))
  {
   CreateClient("client.dll");
     CreateModule("mejors.dll");
    CHange.mejors~>spath;
    // wait to do the real suspend not 100% suspend , at real time
    Allow(105);
    DisAllow.Allow = 100;
    repeat(100); // reapeat this time to freeze
    if(repeat(100) == value.val(100)) // resume 
    {
     Allow(0);
      DisAllow.Allow = 0; // resumed . > 
    }
  }
  
}
// cont > > . . . .
